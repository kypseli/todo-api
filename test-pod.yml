metadata:
  labels:
    some-label: some-label-value
spec:
  containers:
  - name: golang
    image: golang:1.10
    command:
    - cat
    tty: true
  containers:
  - name: mongo
    image: mongo:4.1
    ports:
    - containerPort: 27017