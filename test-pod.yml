metadata:
  labels:
    some-label: some-label-value
spec:
  containers:
  - name: golang
    image: golang:1.10
    command:
    - cat
    tty: true
  containers:
  - name: mongo
    image: mongo:latest
    ports:
      - '27100:27017'